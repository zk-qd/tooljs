
import { color } from '../variable';
export default {
    // 雷达图字体的显示方式
    name: {
        textStyle: {
            borderRadius: 3,
            padding: [3, 5],
            color: color
        }
    },
}